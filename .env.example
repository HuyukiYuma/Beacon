# Beaconが使用するAIプロバイダーの設定ファイル
# コピーして .env を作成し、実際の値を入力する（.envはGit管理対象外）

# 使用するプロバイダー名（providers/配下のファイル名と一致させる）
AI_PROVIDER=anthropic

# 使用するモデル名（プロバイダーごとに異なる）
AI_MODEL=your-model-name

# 各プロバイダーのAPIキー（実際に使用するプロバイダーの行だけ入力すればよい）
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
